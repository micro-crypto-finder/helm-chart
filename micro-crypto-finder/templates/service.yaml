apiVersion: v1
kind: Service
metadata:
  name: {{ include "micro-crypto-finder.fullname" . }}
  labels:
    {{- include "micro-crypto-finder.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "micro-crypto-finder.selectorLabels" . | nindent 4 }}
