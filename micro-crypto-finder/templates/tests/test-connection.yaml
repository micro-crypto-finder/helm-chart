apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "micro-crypto-finder.fullname" . }}-test-connection"
  labels:
    {{- include "micro-crypto-finder.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "micro-crypto-finder.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
